<%= turbo_stream.replace dom_id(@user), partial: 'users/mobile_step_form', locals: {user: @user} %>
<%= turbo_stream.replace 'recaptcha' do %>
  <div
    id='recaptchaV2'
    data-controller='recaptcha-v2'
    data-recaptcha-v2-site-key-value="<%= ENV['RECAPTCHA_SITE_KEY'] %>">
  </div>
<% end %>